/**
 * /dashboard/positions/[position] — per-position detail (formerly
 * /dashboard/campaigns/[position]).
 *
 * See ../page.tsx for the rename rationale — same underlying data, new URL.
 */

export { default } from '../../campaigns/[position]/page'
